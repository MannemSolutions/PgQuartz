Index
============

PgQuartz is a framework for scheduling PostgreSQL workload across PostgreSQL clusters with an etcd consensus mechanism.
PgQuartz jobs can be defined in yaml format as a series of steps consisting of a series of commands and the checks to verify results.

.. toctree::
   :maxdepth: 2
   :numbered:
   :caption: Contents:

   README
   SCHEDULING
   JOBS
   STEPS
   WHEN
   INSTANCES
   COMMANDS
   CHECKS
   CONNECTIONS
   ETCD
